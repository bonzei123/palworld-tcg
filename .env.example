# Kopie als `.env` neben docker-compose.yml auf dem NAS.
# Nicht committen.

ADMIN_PASSWORD=bitte-ändern
SECRET_KEY=bitte-langen-zufallswert-einfügen
HTTPS_ONLY=false
# DOCKER_CONFIG_FILE=/var/services/homes/DEINUSER/.docker/config.json
